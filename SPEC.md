# SPEC.md — meikyu-rogue

<!-- scaffold template v1.8.0 から展開(2026-08-13)。以後このファイルはプロジェクトが育てる -->

## 1. 目的

ターン制ローグライクのブラウザゲーム。シードから決定的に生成される 5 階層の迷宮を探索し、
最下層(B5)の秘宝「魂珠」を取得すれば勝利、HP が尽きれば敗北。
サーバを持たない静的サイトとして動作し、同一シードなら誰の環境でも同一の迷宮・同一のリプレイになる。

## 2. 機能要求

| ID | 要求 | 優先度 |
|---|---|---|
| F-01 | **決定性**: シード付き PRNG(mulberry32)を注入する。乱数状態は GameState の一部として持ち回り、`step(state, action)` は同一入力に対し常に同一出力を返す。core 内で `Math.random()` / `Date.now()` を呼ばない | must |
| F-02 | **ダンジョン生成**: グリッド 44×26。重ならない矩形部屋(6〜10 室)を配置し、L 字通路で順に接続する。生成は試行予算付き(部屋配置リトライ上限 200)で、予算超過時はその階を作り直す(階リトライ上限 20) | must |
| F-03 | **生成検証(出荷ゲート)**: 生成された各階は、フラッドフィルで「全ての床タイルが連結」かつ「入口から階段・全アイテム・全敵位置に到達可能」を検証してから返す。検証に落ちた階は返さない | must |
| F-04 | **FOV**: 視界半径 6。Bresenham 直線で壁遮蔽を判定する。一度見たタイルは「探索済み」として地形のみ記憶表示(敵・アイテムは視界内のみ描画) | must |
| F-05 | **ターンエンジン**: `step(state, action)` は純関数。アクションは move(8 方向のうち上下左右 4 方向)/ wait / descend / quaff。プレイヤー行動 → 全敵行動の順で 1 ターンが進む。移動先に敵がいれば攻撃(bump attack)になる | must |
| F-06 | **敵 AI**: 敵からプレイヤーへの LOS があり距離 ≤ 8 なら追跡状態になり、以後プレイヤーの現在位置へ貪欲一歩(軸方向優先、塞がっていれば代替軸)で接近する。隣接(上下左右)していれば攻撃する。追跡状態でなければ静止する | must |
| F-07 | **戦闘**: ダメージ = max(1, atk − def) ± 乱数 0〜1(state.rng から消費)。HP 0 以下で死亡。敵を倒すと消滅しメッセージが出る | must |
| F-08 | **アイテム**: 回復薬(踏むと自動取得、quaff で HP+8、最大 HP 上限)/ 剣(取得で atk+2、自動適用)/ 盾(取得で def+1、自動適用)。所持数は薬のみカウント | must |
| F-09 | **階層進行と勝敗**: 階段(>)で B1→B5 へ降りる。B5 には階段の代わりに魂珠(*)があり、取得した瞬間に勝利。プレイヤー HP 0 以下で敗北。勝敗確定後の state は step しても変化しない | must |
| F-10 | **UI**: Next.js 静的エクスポート 1 画面。ASCII グリッド描画、矢印キー / hjkl / wasd で移動、`>` キーまたは移動で階段起動は descend ボタン相当の `.` + 階段上、q で quaff、r でリスタート。HP バー・階数・所持薬数・メッセージログ(直近 5 件)・シード表示と「同じシードで再挑戦 / 新しいシード」を備える | must |

## 3. 非機能要求

| ID | 要求 | 検証方法 |
|---|---|---|
| N-01 | `src/core` は純関数のみ。React / DOM / Node API への依存を持たない | eslint(no-restricted-imports)+ コードレビュー |
| N-02 | `src/core` テストカバレッジ lines/functions/statements ≥ 90%、branches ≥ 85% | `vitest run --coverage` の閾値ゲート |
| N-03 | サーバ API なしで動作(`next build` 静的エクスポートが成功し、out/ のみで遊べる) | `npm run verify` の build ステップ |
| N-04 | 敵数・アイテム数は階が深いほど増える(B1: 敵 3 / B5: 敵 7 を基準に線形) | T-030 系で数値検証 |

## 4. 品質基準

- 決定性: 同一シードで `generateFloor` を 2 回呼ぶと深いレベルで同一の階が得られる(T-011)。
  同一シード・同一アクション列で `step` を再生すると最終 state が一致する(T-021)。
- 生成健全性: シード 1〜50 の全 5 階(計 250 階)で F-03 の検証が成立する(T-014)。
- 予算保証: 生成・AI に無限ループが存在しない(全探索に上限があることをテストで固定)。

## 5. スコープ外

- リアルタイム要素・アニメーション・効果音
- セーブ/ロード(リプレイはシード+操作列で再現できるため)
- 斜め移動、遠隔攻撃、魔法、満腹度
- モバイル用タッチ操作(キーボード前提)
- スコアランキング・サーバ通信
